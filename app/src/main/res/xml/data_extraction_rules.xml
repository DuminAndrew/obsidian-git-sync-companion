<?xml version="1.0" encoding="utf-8"?>
<!--
    Android 12+ (API 31) data-extraction rules. We exclude all shared prefs
    and files from both cloud backup and device-to-device transfer so the
    encrypted PAT and sync state stay local to the device.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
    </device-transfer>
</data-extraction-rules>
